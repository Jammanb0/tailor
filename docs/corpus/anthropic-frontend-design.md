# frontend-design 원문 감사 (anthropic-frontend-design)

| | |
|---|---|
| 저작자 / 라이선스 | Anthropic / Apache-2.0 (`LICENSE.txt` 실물 확인) |
| 원문 | https://github.com/anthropics/skills/tree/main/skills/frontend-design |
| 판본 | `main`, `skills/`를 건드린 최신 커밋 `f379e5a` / 취득 2026-08-18 |
| 대조 지문 | `SKILL.md` md5 `31c633612585bf2dd4e6d1103afc9f33`, 8,260바이트, 55줄 |
| 감사일 | 2026-08-18 |

> 이 문서는 원문의 사본이 아니라 **감사 기록**이다. 원문 문장은 옮기지 않고
> 한국어로 요약하며, 요약하면 값이 사라지는 것(hex·개수·클래스명·속성명)만
> 원문 그대로 적는다. 인용 위치는 원문 줄번호로 표기한다.

> **2026-08-18 증보.** 초판은 항목 대조표 중심이라 "원문이 어떻게 쓰였는가"가
> 빠져 있었다. 앞에 분석 4장(2. 문서 해부 / 3. 산출물 형식 / 4. 구체 예시 전수 /
> 5. 문장 기법)을 붙이고 기존 내용은 그대로 뒤로 옮겼다.
> **장 번호 이동: 옛 2→6, 3→7, 4→8, 5→9, 6→10.** 항목 id(`D-17` 등)는 그대로다 —
> 코퍼스 `auditIds`가 이 id를 가리키므로 바뀌면 안 된다.
> 원문은 재취득해 md5가 초판과 동일함을 확인했다(같은 판본).
>
> **9-4(의도적 제외 권장)는 이 증보 시점에 무효다.** "원문에서 빼는 것은 없다 —
> 옮길 수 없으면 번역한다"로 원칙이 바뀌었다(3-4 참조). 기록은 판단 이력으로 남긴다.

**결론부터.** 원문에서 **30개 지시**를 추려냈는데, 현 코퍼스의 frontend-design
패턴은 **5개**다. 문제는 개수가 아니라 **어느 쪽이 살아남았는가**다.
살아남은 5개는 전부 "의도적으로 하라"류의 태도 문장이고, **검증 가능한 구체값은
단 하나도 코퍼스에 없다** — AI 디자인이 몰리는 세 가지 기본값(`#F4F1EA` 등),
색 4–6개·서체 2역할 이상이라는 토큰 개수, 품질 바닥선 3항목, ASCII 와이어프레임.
「글쓰기」 섹션은 5문단 통째로 없다.

**별개의 발견 하나.** 이 소스가 `reference`(참조형) **구조 골격의 출처**로도
태그돼 있는데, 원문에 참조형 구조가 없다. 내용 압축과 무관한 문제이고,
사용자 화면 표기에 직접 걸린다 — 8-5 참조.

## 1. 원문은 어떤 문서인가

- **골격 판정: 규율형(discipline)**. 번호 붙은 단계가 없고, 절대 다수가 "무엇을
  지켜라 / 무엇을 하지 마라"의 원칙 문장이다. 단 `## Process:` 섹션 하나만
  절차형 성격(2패스)을 띤다 — **순수 규율형이 아니라 규율형 안에 절차 블록이
  박힌 형태**다.
- **섹션 구성** (6개): 도입(스튜디오 디자인 리드 역할 부여) / Ground it in the
  subject / Design principles / Process: brainstorm…critique again / Restraint
  and self-critique / More on writing in design.
- **독자 가정**: 코드를 직접 쓰는 실행자. CSS 선택자 특정성, 스크린샷 촬영,
  thinking 활용 등 실행 환경을 전제한 지시가 섞여 있다.
- **자체 규정 출력 형식: 있다.** 1패스 산출물의 형식을 문서가 직접 못박는다 —
  색은 명명된 hex 4–6개, 타입은 2개 이상 역할, 레이아웃은 한 문장 산문 +
  ASCII 와이어프레임, 시그니처 1개. **이 문서에서 가장 구체적인 대목이고,
  현 코퍼스가 통째로 놓친 대목이기도 하다.**

## 2. 문서 해부 — 무게가 어디에 실려 있나

문단 단위로 글자 수를 세어 비중을 냈다(frontmatter·제목 제외, 본문 7,721자).
**눈으로 읽으면 「Design principles」가 이 문서의 본체처럼 느껴지는데, 실제로
가장 무거운 섹션은 「Process」다.**

| 섹션 | 문단 | 글자 | 비중 |
|---|---|---|---|
| (도입) 역할 부여 | 1 | 413 | 5.3% |
| Ground it in the subject | 1 | 536 | 6.9% |
| Design principles | 6 | 2,009 | 26.0% |
| **Process: brainstorm…critique again** | 5 | **2,448** | **31.7%** |
| Restraint and self-critique | 1 | 752 | 9.7% |
| More on writing in design | 5 | 1,563 | 20.2% |

여기서 세 가지가 드러난다.

1. **「Process」가 최대 섹션이고, 그 안에 이 문서의 구체값이 몰려 있다.**
   D-17(세 가지 기본 룩), D-19(토큰 시스템 4종), D-20(2패스 검토)이 전부 이
   섹션이다. 즉 **분량 1위 섹션과 구체성 1위 섹션이 같다** — 원저자가 무게를
   실은 곳이 분명하다. 현 코퍼스는 정확히 이 섹션에서 가장 많이 잃었다.
2. **「More on writing in design」이 2위(20.2%)인데 코퍼스에는 0개다.** 문단
   수로도 5개로 「Design principles」와 맞먹는다. 부록이 아니라 본편이다(8-2).
   (8-2의 "26%"는 문단 수 기준 5/19이고 여기 20.2%는 글자 수 기준이다 —
   같은 사실의 다른 척도이지 어긋난 값이 아니다.)
3. **문단 하나짜리 섹션이 3개**(도입·Ground·Restraint)인데, 그중 둘이 코퍼스에
   살아남았다. **짧은 섹션일수록 요약에서 살아남았다** — 요약이 내용의 중요도가
   아니라 문단의 압축 용이성을 따라간 흔적이다.

## 3. 원문이 규정하는 산출물 형식

"태도"가 아니라 **결과물의 모양을 못박은 대목**만 따로 모은다. 코퍼스가 통째로
놓친 부분이고, 스키마의 `format`/`options`/`examples` 칸이 필요해진 이유다.

### 3-1. 1패스 산출물 — 토큰 시스템 4종 (D-19, L33)

| 조각 | 원문이 규정한 형식 |
|---|---|
| Color | **명명된 hex 4–6개**로 팔레트를 기술 |
| Type | **2개 이상의 역할** — ⓐ 절제해 쓰는 개성 있는 디스플레이 ⓑ 어울리는 본문 ⓒ 필요시 캡션·데이터용 유틸리티 |
| Layout | **한 문장 산문 서술 + ASCII 와이어프레임**으로 안을 비교 |
| Signature | 이 페이지가 기억될 **단 하나의** 요소 |

세 가지가 동시에 규정돼 있다는 점이 중요하다 — **개수**(4–6, 2+, 1),
**구성**(색/타입/레이아웃/시그니처 4조각), **표현 수단**(ASCII 와이어프레임).
현 코퍼스의 `plan-then-critique`에는 이 중 아무것도 없다.

### 3-2. 2패스 검토 — 판정 절차 (D-20, L35)

형식만이 아니라 **판정법까지** 규정한다: 계획의 각 부분이 "비슷한 페이지면
아무거나 나올 법한 기본값"으로 읽히는지 보되, **비슷한 프롬프트를 실제로 굴려
같은 결과에 도달하는지 확인**하라고 한다. 주관적 판단을 **재현 가능한 실험**으로
바꾼 것이다. 그리고 고쳤으면 **무엇을 왜 바꿨는지 말하라**고 요구한다 — 결과물에
근거가 남는다.

### 3-3. 품질 바닥선 — 3항목 체크리스트 (D-24, L43)

**모바일까지 반응형 / 키보드 포커스 가시 / `prefers-reduced-motion` 존중.**
"티내지 않고" 지키라는 단서까지 붙어 있다(문서에 자랑처럼 적지 말라는 뜻).
3개로 닫힌 목록이라 결과물에서 그대로 대조된다.

### 3-4. 형식이 아닌 것 — 구분해 둘 것

`## Process` 안에는 **실행 환경 전제**도 섞여 있다: thinking 안에서 반복(D-22),
스크린샷 촬영(D-25). 이것들은 "산출물 형식"이 아니라 "그 스킬이 돌아가는 환경"에
대한 것이다. **다만 2026-08-18 원칙에 따라 제외 대상은 아니다** — 버리는 대신
번역한다(9-4의 판단이 이 원칙으로 뒤집혔다).

## 4. 구체 예시 전수 — 원문에 등장하는 값 전부

요약하면 소멸하는 것들이다. **원문에 나온 형태 그대로** 적는다.

| 값 | 어디서 | 무엇으로 쓰였나 |
|---|---|---|
| `#F4F1EA` **근처의 따뜻한 크림 배경** + 고대비 세리프 디스플레이 + 테라코타 액센트 | D-17, L31 | **피해야 할 기본값 ⓐ** |
| 니어블랙 배경 + 밝은 **애시드 그린 / 버밀리언** 액센트 하나 | D-17, L31 | **피해야 할 기본값 ⓑ** |
| 헤어라인 괘선 + **`border-radius: 0`** + 신문식 조밀 다단 | D-17, L31 | **피해야 할 기본값 ⓒ** |
| 큰 숫자 + 작은 라벨 + 보조 스탯 + 그라데이션 액센트 | D-06, L17 | **템플릿 답안**(히어로) |
| `01 / 02 / 03` 번호 마커 | D-11, L21 | 내용이 실제 순서일 때만 허용 |
| `.section`(타입 기반) ↔ `.cta`(요소 기반) | D-21, L37 | 서로 상쇄되는 CSS 선택자 예 |
| "Save changes" ↔ **"Submit"** | D-28, L51 | 능동태 대조쌍(뒤가 나쁜 예) |
| "Publish" 버튼 → **"Published"** 토스트 | D-28, L51 | 이름 일관성의 예 |
| "알림을 관리" ↔ **"웹훅 설정"을 관리** | D-27, L49 | 사용자 어휘 대조쌍 |
| hex **4–6개** / 서체 **2역할 이상** / 시그니처 **1개** | D-19, L33 | 토큰 개수 |
| 반응형·키보드 포커스·`prefers-reduced-motion` | D-24, L43 | 품질 바닥선 3항목 |
| "그림 한 장이 토큰 1000개 값" | D-25, L43 | 스크린샷 근거 |
| 샤넬 — 나가기 전 거울 보고 **액세서리 하나 빼라** | D-23, L43 | 절제의 비유 |

**전부 13개. 현 코퍼스에 들어 있는 것은 0개다.**

극성이 뒤섞여 있다는 점을 특히 볼 것 — `#F4F1EA`와 `border-radius: 0`은
**하지 말라는 쪽**이고, `4–6개`와 3항목 체크리스트는 **하라는 쪽**이다. 표시
없이 한 목록에 담으면 정확히 반대로 읽힌다. 스키마에서 `PatternExample.polarity`를
선택 항목이 아니라 **필수**로 만든 근거가 이것이다.

## 5. 문장 기법 — 왜 이렇게 썼는가

이 문서가 "지켜지는" 이유를 문장 차원에서 본다. 코퍼스에 옮길 때 **내용만 옮기고
기법을 버리면 같은 말이 지시력을 잃는다.**

### 5-1. 규칙마다 예외를 붙여 회피를 막는다

가장 두드러진 기법이다. 강한 주장 뒤에 반드시 **반대 방향의 단서**가 온다.

- "기본값을 쓰지 마라" → **"단 브리프가 그 룩을 요구하면 브리프의 말이 이긴다"**(D-18)
- "대담함은 한 곳에만" → **"다만 모험하지 않는 것 자체가 위험일 수 있다"**(D-23)
- "모션을 활용하라" → **"다만 과한 애니메이션은 AI가 만든 느낌을 준다"**(D-14)

예외를 미리 적어두면 **"이 경우는 다르니까"라는 회피 논리가 설 자리가 없어진다.**
읽는 쪽이 규칙을 스스로 무력화하는 경로를 원저자가 먼저 닫아둔 것이다.

### 5-2. 금지를 값으로 못박아 반증 가능하게 만든다

"흔한 디자인을 피하라"는 판정 불가능한 말이다. 원문은 이것을 **`#F4F1EA`,
애시드 그린, `border-radius: 0`**으로 바꿨다(D-17). 그 순간 결과물을 열어
**확인할 수 있는 주장**이 된다. 같은 기법이 D-06(히어로 템플릿 답안 조합),
D-11(`01 / 02 / 03`)에서 반복된다.

**이것이 이 문서에서 가장 배울 만한 기법이고, 현 코퍼스가 정확히 이 층만
걷어냈다** — 태도 문장("진부한 기본값을 피한다")은 남기고 값은 버렸다.

### 5-3. 대조쌍으로 적어 판정선을 만든다

「글쓰기」 섹션의 주된 형태다. `"Save changes," not "Submit"`처럼 **좋은 예와
나쁜 예를 한 문장에 붙여** 쓴다. 규칙을 설명하지 않고 **경계를 보여준다** —
읽는 쪽이 새로운 사례를 만났을 때 어느 쪽에 가까운지로 판단할 수 있다.

### 5-4. 판정을 절차로 환원한다

"고유한가?"는 주관이다. 원문은 **"비슷한 프롬프트를 굴려 같은 데 도달하는지
확인하라"**(D-20)로 바꾼다. 마찬가지로 "과한가?"는 **"거울 보고 하나 빼라"**
(D-23)로 바꾼다. 추상적 기준을 **해볼 수 있는 동작**으로 내린다.

### 5-5. 역할 부여로 기준선을 인격화한다

첫 문단(D-01)이 "작은 스튜디오의 디자인 리드"이고, 여기에 **"이 클라이언트는
이미 템플릿 같은 제안을 반려했다"**는 설정이 붙는다. 품질 기준을 규칙으로
나열하는 대신 **상황으로 심어놓은 것** — 이후 모든 판단이 "그 클라이언트가
받아줄까"로 수렴한다.

### 5-6. 요약이 무엇을 죽이는지

위 다섯 기법은 전부 **문장의 길이에 기대어 있다.** 예외절·대조쌍·구체값은 모두
"덧붙인 부분"이라, 한 줄 요약으로 압축하면 **가장 먼저 잘려나가는 자리**에 있다.
현 코퍼스 `detail` 중앙값이 69자라는 사실(9-5)과 정확히 맞물린다 —
**분량 상한이 곧 기법 제거였다.**

## 6. 원문이 규정하는 것 — 항목 단위 분해

`구체성` = 요약하면 값이 죽는 정도. `kind` = `artifact`(완성된 SKILL.md에서
확인 가능) / `process`(작성 과정에만 작용, 결과물로 검증 불가). 근거는 원문
줄번호.

| # | 원문 지시 | 구체성 | kind | 근거 |
|---|---|---|---|---|
| D-01 | 작은 스튜디오의 디자인 리드로서 임한다 — 클라이언트는 이미 템플릿 같은 제안을 반려했고, 고유한 관점에 돈을 내고 있다 | 역할 설정 | process | L9 |
| D-02 | **정당화할 수 있는 미적 모험을 하나는 감수한다** | — | artifact | L9 |
| D-03 | 브리프가 주제를 특정하지 않으면 **먼저 직접 고정한다**: 구체적 주제 1개 · 청중 · 페이지의 단일 목적을 정하고 그 선택을 명시 | **3항목 지정** | process | L13 |
| D-04 | 주제의 세계 — 소재·도구·산물·업계 용어 — 가 고유한 선택이 나오는 곳이다. 브리프의 실제 내용으로 끝까지 만든다 | — | artifact | L13 |
| D-05 | 히어로는 주장이다. 주제 세계에서 가장 특징적인 것으로 연다 — 헤드라인·이미지·애니메이션·라이브 데모·인터랙티브 순간 중 형식은 자유 | 형식 5종 예시 | artifact | L17 |
| D-06 | **"큰 숫자 + 작은 라벨 + 보조 스탯 + 그라데이션 액센트"는 템플릿 답안**이다. 정말 최선일 때만 쓴다 | **반례 조합 명시** | artifact | L17 |
| D-07 | 디스플레이와 본문 서체를 의도적으로 짝짓는다 — 다른 프로젝트에서 쓰던 같은 패밀리를 반사적으로 쓰지 않는다 | — | artifact | L19 |
| D-08 | **타입 스케일을 명확히 잡고 굵기·너비·자간을 의도적으로 설정한다** | **4개 축 지정** | artifact | L19 |
| D-09 | 타이포 처리 자체가 기억에 남는 요소여야 한다 — 내용을 실어 나르는 중립적 수단이 아니다 | — | artifact | L19 |
| D-10 | 구조는 정보다. 번호·아이브로우·구분선·라벨 같은 구조 장치는 내용에 관한 참인 무언가를 담아야 하고 장식이면 안 된다 | 장치 4종 열거 | artifact | L21 |
| D-11 | **번호 마커(`01 / 02 / 03`)는 내용이 실제로 순서일 때만** — 진짜 프로세스나 시점이 찍힌 타임라인처럼 순서가 독자에게 정보를 줄 때. 넣기 전에 타당한지 자문한다 | **반례 + 예외 조건** | artifact | L21 |
| D-12 | 모션은 **쓸 곳과 쓸지 여부**를 함께 판단한다 — 페이지 로드 시퀀스, 스크롤 트리거 등장, 호버 마이크로 인터랙션, 앰비언트 분위기 | 용처 4종 열거 | artifact | L23 |
| D-13 | 흩뿌린 효과보다 연출된 한 순간이 대개 더 세게 꽂힌다 | — | artifact | L23 |
| D-14 | 애니메이션이 과하면 **"AI가 만든" 느낌**에 기여한다 | — | artifact | L23 |
| D-15 | 복잡도를 비전에 맞춘다 — 맥시멀리즘 방향은 정교한 실행을, 미니멀 방향은 여백·타입·디테일의 정밀함을 요구한다. 우아함은 택한 비전을 잘 실행하는 것 | 두 방향 각각의 요구 | artifact | L25 |
| D-16 | 브리프에 실제 카피가 없으면 직접 쓴다. **카피도 디자인만큼이나 템플릿 느낌을 낼 수 있다** | — | artifact | L27 |
| D-17 | **캘리브레이션 — 현재 AI 디자인이 몰리는 세 가지 룩**: ⓐ 따뜻한 크림 배경(`#F4F1EA` 근처) + 고대비 세리프 디스플레이 + 테라코타 액센트 ⓑ 니어블랙 배경 + 밝은 애시드 그린 또는 버밀리언 액센트 하나 ⓒ 헤어라인 괘선 · `border-radius: 0` · 신문식 조밀 다단의 브로드시트 레이아웃. 셋 다 어떤 브리프에는 정당하지만 **선택이 아니라 기본값**이며 주제와 무관하게 나온다 | **hex·색이름·CSS값까지 명시** | artifact | L31 |
| D-18 | **브리프가 방향을 지정했으면 그대로 따른다 — 브리프의 말이 항상 이긴다.** 위 세 룩 중 하나를 요구할 때도 마찬가지. 브리프가 비워둔 축에서만, 그 자유를 기본값에 쓰지 않는다 | **우선순위 규칙** | process | L31 |
| D-19 | **1패스 — 토큰 시스템 4종**: Color = 명명된 hex **4–6개**로 팔레트 기술 / Type = **2개 이상 역할**(절제해 쓰는 개성 있는 디스플레이, 어울리는 본문, 필요시 캡션·데이터용 유틸리티) / Layout = 한 문장 산문 + **ASCII 와이어프레임**으로 안 비교 / Signature = 이 페이지가 기억될 단 하나의 요소 | **개수·형식까지 명시** | process | L33 |
| D-20 | **2패스 — 만들기 전에 계획을 브리프에 비춰 검토한다.** 어느 부분이든 "비슷한 페이지면 아무거나 나올 법한 기본값"으로 읽히면(비슷한 프롬프트를 실제로 굴려 같은 데 도달하는지 확인) 고치고, **무엇을 왜 바꿨는지 말한다**. 고유성을 확인한 뒤에야 코드를 쓰고, 수정된 계획을 그대로 따르며 **모든 색·타입 결정을 계획에서 파생시킨다** | **자기검증 절차 + 판정법** | process | L35 |
| D-21 | **CSS 선택자 특정성 주의** — 클래스가 서로 상쇄되기 쉽다. 특히 타입 기반 `.section` 과 요소 기반 `.cta` 조합, 그리고 섹션 간 padding/margin에서 자주 발생 | **클래스명·발생 지점 명시** | artifact | L37 |
| D-22 | 계획과 반복은 되도록 thinking 안에서 하고, 사용자에게는 만족시킬 자신이 생겼을 때만 보여준다 | — | process | L39 |
| D-23 | **대담함은 한 곳에 쓴다.** 시그니처 요소 하나만 기억에 남게 하고 주변은 조용하고 규율 있게, 브리프에 복무하지 않는 장식은 잘라낸다. **다만 모험하지 않는 것 자체가 위험일 수 있다** | 양방향 규정 | artifact | L43 |
| D-24 | **품질 바닥선을 티내지 않고 지킨다: 모바일까지 반응형 · 키보드 포커스 가시 · `prefers-reduced-motion` 존중** | **3항목 체크리스트** | artifact | L43 |
| D-25 | 만들면서 자기 결과물을 비판한다. 환경이 지원하면 **스크린샷을 찍어서** — "그림 한 장이 토큰 1000개 값". 시도한 것을 메모해두면 다음 패스에 도움이 된다 | 수단 명시 | process | L43 |
| D-26 | **글쓰기 ①** 단어는 이해를 쉽게, 따라서 사용을 쉽게 하려고 존재한다. 카피는 장식이 아니라 **디자인 재료**이며 여백·색과 같은 의도성으로 다룬다. 쓰기 전에 "이 디자인이 무엇을 말해야 하는가"를 먼저 묻는다 | — | artifact | L47 |
| D-27 | **글쓰기 ②** 화면 사용자 쪽에서 쓴다. 사람이 다루고 알아보는 이름으로 부르고 시스템 구현 방식으로 부르지 않는다 — **"알림을 관리"하지 "웹훅 설정"을 관리하지 않는다**. 파는 말투 대신 하는 일을 평범하게. **영리한 것보다 구체적인 게 항상 낫다** | **반례 대조** | artifact | L49 |
| D-28 | **글쓰기 ③** 능동태가 기본. 컨트롤은 눌렀을 때 벌어질 일을 그대로 적는다 — **"Save changes", "Submit"이 아니라**. 한 동작은 흐름 전체에서 같은 이름을 유지한다 — **"Publish" 버튼은 "Published" 토스트를 낸다**. 인터페이스의 어휘가 길잡이 표지판이다 | **문자열 예시 2쌍** | artifact | L51 |
| D-29 | **글쓰기 ④** 실패와 빈 화면은 분위기가 아니라 **방향을 주는 순간**이다. 무엇이 잘못됐고 어떻게 고치는지를 사람이 아닌 **인터페이스의 목소리**로 설명한다. **에러는 사과하지 않고, 무슨 일이 났는지 모호하게 말하지 않는다.** 빈 화면은 행동하라는 초대다 | **금지 규칙 2개** | artifact | L53 |
| D-30 | **글쓰기 ⑤** 레지스터는 대화체로 조율한다 — 평범한 동사, 문장형 대소문자, 군더더기 없음, 브랜드와 청중에 맞춘 톤. **요소 하나는 정확히 한 가지 일만** — 라벨은 라벨하고 예시는 보여주며, 무엇도 몰래 두 몫을 하지 않는다 | 4개 축 + 단일책임 | artifact | L55 |

> 총 30개. `artifact` 22 / `process` 8.

## 7. 현 코퍼스와의 대조

현 코퍼스의 `design` 카테고리 패턴 8개 중 **frontend-design 출처는 5개**
(나머지 3개는 theme-factory). `detail` 길이는 57·63·69·85·106자, **중앙값 69자**.

판정: `유지`(충실히 반영) / `압축`(id는 있으나 원문보다 약함) / `누락` / `변형`.

| 원문 | 현 패턴 id | 판정 | 무엇이 남고 무엇이 빠졌나 |
|---|---|---|---|
| D-04, D-05, D-06 | `ground-in-subject` | **압축** | 주제 세계에서 끌어낸다 + 히어로 + 기본값 회피가 **한 패턴에 뭉쳐 있다**. "큰 숫자+그라데이션"으로 줄면서 D-06의 4요소 조합이 2개로 깎였고, 히어로 형식 5종(D-05)은 사라졌다 |
| D-03 | `ground-in-subject` | **누락** | 브리프가 비었을 때 주제·청중·단일 목적을 직접 고정하라는 **선행 절차**가 없다. "주제에서 끌어낸다"만 남아, 주제가 없을 때 무엇을 하라는 답이 빠졌다 |
| D-23, D-10, D-11 | `one-signature-element` | **압축** | 시그니처 집중은 남았다. 번호 마커는 "순서가 실제로 의미를 가질 때만"까지 살아남았으나 `01 / 02 / 03`이라는 **구체 형태와 예외 조건(진짜 프로세스·타임라인)**이 빠졌다. D-23 후반의 **"모험하지 않는 것도 위험"이 통째로 없다** — 절제 쪽만 남아 원문보다 보수적으로 기울었다 |
| D-07 | `deliberate-typography` | **압축** | 짝짓기만 남았다. D-08(타입 스케일·굵기·너비·자간)과 D-09(타이포 자체가 기억 요소)가 없다. 57자로 이 카테고리에서 가장 짧다 |
| D-12, D-13, D-14 | `purposeful-motion` | **압축** | 세 항목이 69자에 압축됐다. 용처 4종(로드 시퀀스·스크롤 등장·호버·앰비언트)이 사라져 **"어디에 쓸지"의 답이 없다**. "쓸지 여부"까지 판단하라는 D-12 전반부도 흐려졌다 |
| D-19, D-20 | `plan-then-critique` | **압축** | "계획→자기비판→구현" 골자만 63자로 남았다. **원문에서 가장 구체적인 D-19 토큰 시스템(hex 4–6, 역할 2+, ASCII 와이어프레임, 시그니처)이 전부 사라졌다.** D-20의 "무엇을 왜 바꿨는지 말한다", "모든 색·타입 결정을 계획에서 파생"도 없다 |
| D-01, D-02 | — | **누락** | 역할 설정과 "정당화 가능한 모험 하나" |
| D-15 | — | **누락** | 복잡도-비전 정합 |
| D-16 | — | **누락** | 카피도 템플릿 느낌을 낸다 |
| **D-17** | — | **누락** | **AI 기본값 세 가지 캘리브레이션(`#F4F1EA`, 애시드그린/버밀리언, 브로드시트+radius 0)** |
| D-18 | — | **누락** | 브리프의 말이 항상 이긴다는 우선순위 규칙 |
| D-21 | — | **누락** | CSS 선택자 특정성 |
| D-22 | — | **누락** | thinking 안에서 반복 |
| **D-24** | — | **누락** | **품질 바닥선 3항목(반응형·키보드 포커스·`prefers-reduced-motion`)** |
| D-25 | — | **누락** | 스크린샷 자기비판 |
| **D-26 ~ D-30** | — | **누락** | **「글쓰기」 섹션 5문단 전체** |
| (문서 전체 구조) | `reference` 골격의 `sourceIds` | **변형** | 코퍼스는 frontend-design을 **참조형(옵션 제공·적용) 골격의 출처**로 태그해 뒀다. 원문은 참조형이 아니다 — 8-5 참조 |

**집계: 유지 0 / 압축 5(원문 12개 항목 흡수) / 누락 18 / 변형 1(구조 골격).**

## 8. 소실된 것

### 8-1. 압축의 정체는 "검증 가능한 것만 골라서 빠졌다"

압축된 5개를 보면 규칙성이 뚜렷하다. **각 패턴에서 태도 문장은 남고 구체값은
빠졌다.** `plan-then-critique`가 대표적이다 — 2패스라는 태도는 남았는데, 그
2패스가 무엇을 산출해야 하는지(hex 4–6개, 역할 2개 이상, ASCII 와이어프레임)는
전부 사라졌다. 남은 63자만 읽고는 계획을 세울 수 없다.

이건 생성 품질에 직접 영향을 준다. "모션은 목적을 갖고 절제한다"(69자)를
프롬프트로 받은 모델은 그 문장을 **거의 그대로 되풀이하는 것 말고 할 수 있는 게
없다**. 반면 "로드 시퀀스·스크롤 등장·호버·앰비언트 중에서 고르고, 흩뿌리지 말고
한 순간에 연출하라"를 받으면 결과물에 선택이 생긴다.

### 8-2. 「글쓰기」 섹션 전체 누락 — 문단 기준 원문의 26%

원문의 본문 문단은 19개인데 그중 5개(L47~L55)가 UI 카피 규칙이다. 코퍼스에
흔적이 없다.
그리고 이 섹션은 **원문에서 가장 검증하기 쉬운 대목**이다 — "Save changes"/
"Submit", "Publish"→"Published", "알림 관리 vs 웹훅 설정", "에러는 사과하지
않는다" 모두 완성된 SKILL.md에서 있는지 없는지 바로 짚을 수 있다.

`voice`(말투·보이스)와 `documentation`(문서 작성) 카테고리가 따로 있어서
그쪽으로 갔을 가능성을 확인해야 한다 — **이번 감사 범위 밖이므로 단정하지 않고
후속으로 넘긴다.** 다만 두 카테고리 어디에도 frontend-design이 `sourceIds`로
달려 있지 않으므로, 갔더라도 출처가 끊긴 상태다.

### 8-3. 가장 뼈아픈 누락은 D-17

원문의 핵심 주장은 "템플릿처럼 보이지 마라"다. 그리고 원문은 그 주장을
**추상적으로 말하지 않고 세 가지 룩을 hex까지 지목해서** 반증 가능하게 만든다.
코퍼스는 주장은 가져왔고 반증 가능한 부분은 버렸다. 결과적으로 Tailor가 생성한
디자인 스킬은 "고유하게 만들라"고 말하지만 **무엇이 흔한지는 알려주지 않는다**.

### 8-4. 왜 이렇게 됐나 (추정)

- **`detail`을 한두 문장으로 쓴다는 암묵적 형식.** 8개 전부 54~106자에 몰려
  있는 건 우연이 아니라 저술 시 잡힌 틀로 보인다. 원문 한 문단을 한 문장에
  넣으려면 구체값부터 버리게 된다.
- **패턴 개수를 소스당 5개 안팎으로 맞춘 흔적.** 30개 항목을 5개로 줄이려면
  묶어야 하고, 묶으면 공통분모인 태도만 남는다.
- 다만 **D-26~D-30(글쓰기)은 압축이 아니라 통째 누락**이라 위 두 가지로
  설명되지 않는다. 카테고리 경계("디자인"에 카피가 들어가나?) 문제일 가능성이
  높고, 이건 8-2의 후속 확인과 함께 봐야 한다.

### 8-5. 구조 골격 출처가 어긋나 있다 (내용 패턴과 별개 문제)

이건 `detail` 압축과 무관한, **별개의 발견**이다.

`structureArchetypes`의 `reference`(참조형 — 옵션 제공·적용) 골격이
`sourceIds`에 `anthropic-frontend-design`을 달고 있다. 그런데 그 골격이 규정하는
흐름은 `## Profile / Options`(선택지 목록) → `## Apply`(승인받고 일관 적용) →
`## Constraints`다. **원문에는 이 구조가 없다** — 고르라고 내놓는 옵션 목록도,
승인받고 적용하는 단계도 없다. 그 흐름은 theme-factory(테마 10종을 제시하고
고르게 함)를 정확히 기술한 것이고, frontend-design은 1장에서 판정했듯
**규율형에 절차 블록이 박힌 문서**다.

왜 문제인가. 이 프로젝트는 결과 화면에서 **내용 패턴 출처와 구조 골격 출처를
일부러 분리 표기**한다 — "디자인 스킬이 규율형 골격을 썼다고 TDD를 내용 참고로
잘못 적지 않기 위해서"였다. 그 분리의 취지가 정작 여기서 깨져 있다. 모델이
참조형 골격을 고르면 화면은 "구조는 frontend-design을 참고했다"고 표기하는데,
**원문에서 그 근거를 짚을 수 없다.** 08-18 실험에서 세운 원칙 — 표기하는 것은
근거를 짚을 수 있어야 한다 — 에 그대로 걸린다.

다만 오해하지 말 것: **08-18 실험의 "구조 골격 12/12"와 모순되지 않는다.**
그 실험이 잰 것은 "모델이 신고한 골격이 실제 문서 구조와 맞는가"였고, 여기서
문제 삼는 것은 "그 골격에 달린 출처 태그가 맞는가"다. ①(코퍼스 저술) 단계는
그 실험에서 "기록 없음"으로 검증 대상 밖이었다. 이번 감사가 그 미검증 구간을
처음 건드린 것이다.

## 9. 반영 제안

이 문서는 **코퍼스를 고치지 않는다.** 아래는 제안이며 채택은 별도 판단이다.

### 9-1. 신규 패턴 (우선순위 순)

| 제안 id | 근거 | kind | 왜 |
|---|---|---|---|
| `avoid-ai-default-looks` | D-17 | artifact | 원문에서 가장 구체적이고, 이 카테고리 주장의 반증 가능한 근거 |
| `quality-floor-unannounced` | D-24 | artifact | 3항목 체크리스트라 결과물에서 바로 확인됨 |
| `design-token-plan` | D-19 | process | 1패스 산출 형식. `plan-then-critique`를 대체하지 않고 그 안의 구체 절차로 분리 |
| `ui-copy-is-design-material` | D-26~D-30 | artifact | 5문단을 1~2개 패턴으로. **단 8-2 확인 후에 위치 결정** |
| `brief-wins` | D-18 | process | 다른 모든 "고유하게 하라"에 대한 예외 규칙. 없으면 지시 무시로 이어짐 |
| `match-complexity-to-vision` | D-15 | artifact | 미니멀/맥시멀 각각의 요구가 다르다는 대칭 규칙 |

### 9-2. 기존 패턴 `detail` 보강

- `deliberate-typography` ← D-08, D-09 (타입 스케일·굵기·너비·자간 / 타이포 자체가 기억 요소)
- `purposeful-motion` ← D-12 용처 4종
- `one-signature-element` ← D-11 `01 / 02 / 03` 구체 형태 + D-23 "모험하지 않는 것도 위험"
- `ground-in-subject` ← D-03(주제 직접 고정 3항목), D-05 히어로 형식 5종, D-06 4요소 조합 복원
- `plan-then-critique` ← D-20 "무엇을 왜 바꿨는지 말한다" + "모든 색·타입 결정을 계획에서 파생"

### 9-3. 구조 골격 출처 정정 (8-5)

`reference` 골격의 `sourceIds`에서 `anthropic-frontend-design`을 **빼는 것**을
제안한다. 원문에 참조형 구조가 없으므로 근거를 짚을 수 없다.

**단, 이건 사용자 화면에 보이는 변경이다** — 참조형 골격이 선택된 생성에서
"구조 골격 출처" 목록이 한 줄 줄어든다. 다른 제안들과 성격이 다르므로 별도로
판단할 것. 남은 두 출처(`anthropic-theme-factory`, `tone-of-voice`)는 이번
감사 범위 밖이라 검증하지 않았고, theme-factory 감사에서 함께 볼 항목이다.

곁들여, `discipline` 골격에 frontend-design을 **추가**하는 것도 검토 대상이다.
1장 판정이 그쪽이기 때문이다. 다만 원문에 절차 블록이 섞여 있어 순수 규율형이
아니고, `discipline` 과선택은 이미 한 번 교정한 이력(`6f40b1a`)이 있으므로
**추가는 신중하게** — 제외와 달리 즉시 권하지 않는다.

### 9-4. 의도적 제외 권장

- **D-21(CSS 선택자 특정성)**: 실행 환경 특유의 디버깅 팁이다. Tailor가 만드는
  것은 SKILL.md 문서이지 CSS가 아니므로, 넣으면 생성물이 스킬 문서가 아니라
  구현 가이드로 흐를 위험이 있다.
- **D-22(thinking 안에서 반복), D-25(스크린샷)**: 실행 도구 전제. 마찬가지.
- **D-01(스튜디오 리드 역할)**: `baseline`의 역할 부여와 중복 가능성. 확인 필요.

### 9-5. `detail` 분량 상한 재검토 (제안)

현 중앙값 69자로는 구체값이 들어가지 않는다. 이번 감사 결과를 반영하려면
**150~250자대**가 필요하다. 다만 코퍼스 전체가 프롬프트에 정적 주입되므로
토큰 비용이 늘고, 그것이 생성 품질에 어떻게 작용하는지는 **측정된 바 없다.**
→ 「다음 실험」에 조건 하나로 추가할 것을 제안한다.

## 10. 판단 기록

다음 소스 감사 때 기준으로 삼기 위한 기록.

- **분해 단위는 "문단"이 아니라 "지시"로 잡았다.** L43 한 문단이 D-23·D-24·D-25
  세 개로 쪼개졌다. 문단으로 세면 원문이 19개로 보이는데, 그러면 품질 바닥선
  3항목 같은 것이 "절제" 항목에 딸린 부속으로 묻힌다.
- **감사 대상에 `patterns`뿐 아니라 `structureArchetypes`의 `sourceIds`도 넣었다.**
  처음엔 내용 패턴만 볼 생각이었는데, 소스 id를 전수 검색하다 8-5가 나왔다.
  **다음 소스 감사에서도 `grep`으로 해당 `source.id`가 코퍼스 어디에 쓰였는지
  먼저 전수 확인하고 시작할 것.**
- **`kind` 판정 기준은 "완성된 SKILL.md만 보고 지켰는지 알 수 있는가"** —
  `445a49d`에서 정한 그대로다. D-03(주제 직접 고정)은 애매했다: 고정한 결과는
  문서에 드러나지만 "브리프가 비었을 때 그렇게 했는지"는 알 수 없다. **작성
  과정의 분기 조건이 판정에 필요하면 `process`**로 정했다.
- **D-02("모험 하나")를 `artifact`로 뒀다.** 모험을 했는지는 주관적이지만,
  "정당화할 수 있는"이 붙어 있어 결과물에 정당화가 있는지로 볼 수 있다고 봤다.
  경계 사례이므로 기록해둔다.
- **누락 판정에 "원래 넣지 말았어야 할 것"을 섞지 않았다.** D-21(CSS)은 9-3에서
  제외를 권했지만 7장에서는 `누락`으로 셌다. **감사는 무엇이 빠졌는지를 기록하고,
  넣을지 말지는 다음 단계에서 판단한다** — 두 판단을 섞으면 "안 넣기로 했으니
  누락이 아니다"로 흐른다.
- **`ground-in-subject` 하나가 원문 항목 3개 + 누락 1개에 동시에 걸린다.**
  대조표에서 한 패턴이 여러 줄에 나오는 것을 허용했다. 1:1 매핑을 강제하면
  "부분적으로 반영됐다"를 표현할 수 없다.
- **원문 대비 개수 비교(30 vs 5)를 그대로 결론으로 쓰지 않았다.** 30개를 전부
  패턴화하는 것이 목표가 아니기 때문이다. 문제로 삼은 것은 개수가 아니라
  **남은 5개가 검증 불가능한 태도 문장만 담고 있다는 편향**이다.

## 재현 방법

원문은 `anthropics/skills` 레포에서 `curl`로 raw 파일을 받아 대조했다.

```bash
curl -sSL https://raw.githubusercontent.com/anthropics/skills/main/skills/frontend-design/SKILL.md -o SKILL.md
md5sum SKILL.md   # 31c633612585bf2dd4e6d1103afc9f33 이면 이 감사와 같은 판본
```

**WebFetch 류의 요약 도구로 받으면 안 된다** — 원문이 아니라 요약본이 돌아와
구체값(hex·개수·클래스명)이 사라진다. 이 감사의 대상이 정확히 그 구체값이다.

---

## [2026-08-21] D-21은 담지 않는다 — E 갈래

**CSS 선택자 특정성 주의**(타입 기반 `.section`과 요소 기반 `.cta`가 서로
상쇄된다)는 **CSS 고유의 함정**이다. Tailor 사용자가 만드는 스킬이 CSS를 다룬다는
보장이 없고, 일반화하면 "규칙끼리 상쇄되지 않게 하라" 정도로 뭉개져 아무것도
말하지 않는 문장이 된다.

**번역이 가능한 다른 환경 전제(D-22 thinking · D-25 스크린샷)와는 다르다.**
그 둘은 알맹이가 환경과 분리되지만, 이것은 CSS라는 환경 자체가 알맹이다.
