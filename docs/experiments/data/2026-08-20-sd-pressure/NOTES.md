# 압박 저항 장치 6개 채운 뒤 육안 확인 (2026-08-20)

A/B가 아니다. 조건 하나로 3건 돌려 눈으로 읽었다.
직전 확인은 `../2026-08-20-sd13/`.

| | |
|---|---|
| 채운 항목 | `SD-04` `SD-05` `SD-06` `SD-17` `SD-22` `SD-24` `SD-32` |
| 새 패턴 | 6개 (108 → 114) |
| 렌더 | 78,286 → **83,533**바이트 |
| 디버깅 카테고리 | 13,957 → **19,204**바이트 (**+38%**) |

감사 8-3이 지목한 것을 채웠다 — "압박 저항 장치가 통째로 빠졌다.
`SD-05`·`06`·`22`·`24`를 합치면 원문 SKILL.md의 **27.7%**인데 코퍼스에는
하나도 없었다." 이 스킬은 문서 전체가 급할 때 무너지는 것을 막으려고 쓰였으므로,
그 장치가 빠지면 남는 것은 절차뿐이고 절차는 급할 때 제일 먼저 버려진다.

## 실린 것 — 위험 신호가 「생각의 형태」로 나왔다

원문이 강조한 것은 행동이 아니라 **머릿속 문장 그대로** 적는 것이다. 3건 전부
그 형식으로 나왔다.

```
run1  "일단 빨리 고치고 조사는 나중에 하자"
run2  "일단 빨리 고치고 조사는 나중에"
run3  "이번만 예외로 하고 넘어가자"는 생각이 든다
```

**run3은 원문에 없는 문장을 만들어냈는데 형식은 정확히 따랐다.** 값을 베낀 것이
아니라 방식을 가져간 것이다.

## ⚠️ 다음에 디버깅을 손댈 때 먼저 확인할 것 두 개

둘 다 **run3 한 건**에서 나왔고, **디버깅 카테고리가 38% 커진 직후**의 관찰이다.

**① 2단계(패턴 분석)가 절차에서 빠졌다**

```
run1  조사 → 패턴 분석 → 가설 → 고치기        ✅
run2  조사 → 비슷한 사례와 비교 → 가설 → 테스트 ✅
run3  조사 → 가설 → 테스트 → 검증              ✗
```

**직전(`../2026-08-20-sd13/`)에는 3건 전부 있었다.** 그 사이에 바뀐 것은 이번
6개 추가뿐이므로, 분량이 늘며 밀려났을 가능성을 배제할 수 없다.

**② run3의 「언제 적용하나」가 얇다** — 압박 관련 항목이 run1 5개 · run2 4개인데
run3은 1개다.

**3건 중 1건이므로 경향이 아니다.** 이 프로젝트에서 두 번 겪은 함정이 정확히
이것이다(3차 회차의 `debugging` F축 +1.33 착시). 단정하지 않고 **다음 확인 때
같은 자리를 먼저 본다.**

관련 커밋: 이번 커밋. 직전 `0a74dde`(SD-13).
